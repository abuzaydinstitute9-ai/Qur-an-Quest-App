class Components{static lessonCard(lesson,completed=false){const status=completed?'completed':'active';const checkmark=completed?'✓':'';return `<div class="lesson-card ${status}" data-lesson-id="${lesson.id}"><div class="lesson-card__header"><div class="lesson-card__arabic">${lesson.arabic}</div><div class="lesson-card__checkmark">${checkmark}</div></div><div class="lesson-card__content"><h3>${lesson.title}</h3><p>${lesson.transliteration}</p></div></div>`}static stageCard(stage){const progressPercent=Math.round(stage.completed/stage.lessons*100);const locked=stage.locked?'locked':'unlocked';const lockIcon=stage.locked?'🔒':'';return `<div class="stage-card ${locked}" data-stage-id="${stage.id}"><div class="stage-card__icon">${stage.icon}</div><h2>${stage.name}</h2><p>${stage.description}</p><div class="stage-card__progress"><div class="progress-bar"><div class="progress-bar__fill" style="width: ${progressPercent}%"></div></div><span class="progress-text">${stage.completed}/${stage.lessons}</span></div>${lockIcon?`<div class="lock-icon">${lockIcon}</div>`:''}</div>`}static progressBar(current,total,label=''){const percent=Math.round(current/total*100);return `<div class="progress-bar"><div class="progress-bar__fill" style="width: ${percent}%"></div><span class="progress-bar__label">${label} ${percent}%</span></div>`}static button(text,onClick='',variant='primary'){const classes=`btn btn--${variant}`;const onClickAttr=onClick?`onclick="${onClick}"`:''return `<button class="${classes}" ${onClickAttr}>${text}</button>`}static badge(text,variant='default'){return `<span class="badge badge--${variant}">${text}</span>`}static statCard(label,value,icon=''){return `<div class="stat-card">${icon?`<div class="stat-card__icon">${icon}</div>`:<div></div>}<div class="stat-card__value">${value}</div><div class="stat-card__label">${label}</div></div>`}static header(title,subtitle=''){return `<div class="page-header"><h1>${title}</h1>${subtitle?`<p>${subtitle}</p>`:''}</div>`}}