class ThemeManager{constructor(){this.currentTheme=this.loadTheme();this.init()}init(){this.applyTheme(this.currentTheme);this.setupToggle()}loadTheme(){const saved=localStorage.getItem('qg_theme');if(saved)return saved;return window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'}applyTheme(theme){document.documentElement.setAttribute('data-theme',theme);localStorage.setItem('qg_theme',theme);this.currentTheme=theme}toggle(){const newTheme=this.currentTheme==='dark'?'light':'dark';this.applyTheme(newTheme);this.updateToggleButton()}setupToggle(){const toggleBtn=document.getElementById('theme-toggle');if(toggleBtn){toggleBtn.addEventListener('click',()=>this.toggle());this.updateToggleButton()}}updateToggleButton(){const toggleBtn=document.getElementById('theme-toggle');if(toggleBtn){toggleBtn.textContent=this.currentTheme==='dark'?'☀️':'🌙'}}}const themeManager=new ThemeManager();